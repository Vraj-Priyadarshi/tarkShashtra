# Application Configuration
spring.application.name=TarkShastra

# Server Configuration
server.port=8080

# Database Configuration (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/tarkshastra_db
spring.datasource.username=root
spring.datasource.password=Khushi79#
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=true
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT Configuration
jwt.secret=pFNUQZL21lWdInoOCazn195POV9DzkQpaMGIJshNfZKFkIEeSIHsSv8ewdgV7mEflkAvpDyds3shJ4sAp8kgVQ==
# 6 hours (6 * 60 * 60 * 1000)
jwt.expiration-ms=21600000

# CORS Configuration
cors.allowed-origins=http://localhost:5173
cors.allowed-methods=GET,POST,PUT,DELETE,PATCH,OPTIONS
cors.allowed-headers=*
cors.exposed-headers=Authorization
cors.allow-credentials=true
cors.max-age=86400

# Email Configuration (Gmail)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=teamimcodeable@gmail.com
spring.mail.password=yjswxmqjieojfkvv
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=3000
spring.mail.properties.mail.smtp.timeout=3000
spring.mail.properties.mail.smtp.writetimeout=3000

# Email sender configuration
app.email.from=teamimcodeable@gmail.com

# CSV upload default password (all CSV-uploaded users get this; they must change on first login)
app.csv.default-password=TarkShastra@123
app.frontend.url=http://localhost:5173

# Logging Configuration
logging.level.root=INFO
logging.level.com.tarkshastra.app=DEBUG
logging.level.org.springframework.security=DEBUG

# ML Service
ml.service.base-url=http://localhost:8000
ml.service.timeout-ms=10000

# Risk thresholds (defaults)
risk.threshold.low=35.0
risk.threshold.medium=55.0

# Consistency streak thresholds
streak.attendance-threshold=75.0
streak.assignment-threshold=80.0
streak.lms-threshold=50.0

# File upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Pagination defaults
pagination.default-page-size=20
pagination.max-page-size=100
